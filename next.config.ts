import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The floating dev-tools badge overlaps the bottom tab bar on mobile.
  devIndicators: false,
};

export default nextConfig;
